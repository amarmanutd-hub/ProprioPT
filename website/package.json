{
  "name": "proprio-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:site": "vite --port 5173 --strictPort",
    "build:demo": "node scripts/build-demo.mjs",
    "build": "vite build",
    "build:full": "npm run build:demo && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vite": "^6.2.0"
  }
}
